# Observer Daily Iteration Plan — 2026-04-19

> Auto-generated by Observer cron
> Full learnings ledger: `.learnings/observer/`

---

## 📊 Today's Execution Overview

| Metric | Value |
|--------|-------|
| Total sessions | 1 visible session |
| Completed successfully | 0 (0%) |
| Timed out / failed | 0 confirmed session failures in retrieved history |
| Gateway errors total | 112 counted anomalies/events (15 timeout, 80 candidate_failed, 17 embedded failover) |
| Fallback triggered | 17 |

---

## 🔴 Infrastructure Anomalies

> Infrastructure layer: Gateway log analysis

- **[high]** FailoverError timed out appeared 15 times, concentrated around local embedded runs and slug-generator tasks. ⚠️ recurring
- **[medium]** candidate_failed appeared 80 times, largely from OpenAI API-key auth mismatches and local model load failures. ⚠️ recurring
- **[medium]** embedded run failover appeared 17 times, mostly from qwen3.6 and gemma4 fallback churn. ⚠️ recurring
- **[medium]** Slow gateway requests exceeded 2000ms 10 times, including several 120000ms timeout warnings and agent.wait calls above 400s.
- ✅ Commands log still showed 6 normal deliveries, so ingress itself did not appear broken.

---

## 🟠 Agent Execution Anomalies

> Agent layer: session analysis

- ✅ Only the current cron session was visible via `sessions_list` during this run.
- ✅ No session-level tool failures were present in the retrieved `sessions_history`.
- ⚠️ Because session visibility was limited to one live session, agent-wide conclusions are low-confidence and rely mostly on gateway evidence.

---

## 💡 Suggested Changes Today

### 1. Remove API-key-only OpenAI routes from fallback chains
- **Type**: config
- **Trigger**: 80 `candidate_failed` entries from `openai/gpt-5.4*` routes without `OPENAI_API_KEY`
- **Suggested action**: Switch intended OAuth routes to `openai-codex/gpt-5.4`, or remove `openai/gpt-5.4*` from automated fallback candidates until credentials exist.
- **Expected effect**: Cuts noisy fallback churn and avoids guaranteed auth failures.
- **Risk level**: low

### 2. Treat local qwen3.6 and gemma4 paths as degraded for embedded tasks
- **Type**: model
- **Trigger**: repeated timeout and HTTP 500 model-load failures tied to embedded runs
- **Suggested action**: Review local model capacity and route latency-sensitive embedded tasks, especially slug generation and cron helpers, to a more stable candidate first.
- **Expected effect**: Fewer timeout cascades and less multi-step failover overhead.
- **Risk level**: medium

### 3. Inspect subagent completion announce path
- **Type**: skill
- **Trigger**: multiple 120000ms timeout warnings and retries during completion announcements
- **Suggested action**: Review the announce flow for retry policy, websocket wait behavior, and whether completion notices can use a lighter path.
- **Expected effect**: Reduced long-tail latency and fewer blocked completion acknowledgements.
- **Risk level**: medium

---

## 🚫 Not Changing Today

### 1. Cron schedules or observer skill logic
- **Reason**: Today's evidence points to infrastructure and model-routing issues, not a broken observer workflow.
- **Revisit when**: Only if the same anomalies persist after routing and gateway fixes.

### 2. Feishu ingress handling
- **Reason**: Commands were still delivered normally, so ingress is not the primary fault line.
- **Revisit when**: Commands log shows delivery drops or user-facing intake failures.

---

## 🔑 Needs Human Decision

| Item | Context | Options |
|------|---------|---------|
| Promote recurring infrastructure rules | `candidate_failed`, `embedded_run_failover`, and `failover_timed_out` all exceed the promote threshold | A: promote routing rules into long-term config guidance / B: keep as watchlist until one more day confirms pattern |
| Reorder default model candidates | Local qwen3.6/gemma4 are repeatedly timing out under embedded load | A: prioritize hosted stable model for embedded tasks / B: keep current order and tune local infra first |
| Fix auth mismatch strategy | OpenAI OAuth exists, but fallback chain still requests API-key-only routes | A: standardize on `openai-codex/*` for OAuth use / B: add real API keys and keep both route families |

---

## 📁 Files Updated Today

- `.learnings/observer/plans/2026-04-19-iteration-plan.md`
- `.learnings/observer/index.json`
- `.learnings/observer/raw/2026-04-19-sessions.jsonl`
- `.learnings/observer/raw/2026-04-19-gateway.jsonl`
- `.learnings/observer/infrastructure/2026-04-19-failover-timeouts-and-embedded-fallbacks.md`
- `.learnings/observer/infrastructure/2026-04-19-openai-provider-auth-fallbacks.md`
- `.learnings/observer/infrastructure/2026-04-19-gateway-slow-request-bursts.md`
